AOP 적용방식 종류
컴파일 시점, 클래스로딩, 런타임(프록시)

컴파일 시점: .java 파일을 .class로 변환하는 시점에 AspectJ컴파일러가 부가 기능을 추가해서 넣어주는 방법인데 별도 컴파일러가 필요하고 복잡
클래스 로딩:   .clss파일을 jvm 내부의 클래스로더에 보관하는데 이때 .class파일을 조작한다음 jvm에 올리는 방법.  클래스 로더 조작기를 지정해야하는데 번거럽고 운영이어려움.
런타임 시점: main class 가 실행되고 난 이후. 메서드 단위로만 joinpoint 부여가능

추가로 스프링은 AspectJ를 직접사용하는 것이아니라 문법만 차용하고 프록시 AOP를 적용.
