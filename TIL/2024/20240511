item13 clone 재정의는 주의해서 진행하라

cloneable으로부터 파생되는 문제점을 봤을 때,
새로운 인터페이스를 만들 때는 절때 Cloneable을 확정해선 안되고, 새로운 클래스도 이를 구현해서도 안됨.

final 클래스라먄 Cloneable을 구현해도 위험이 크진않지만, 성능 최적화 관점에서 검토환 후
드물게 허용해야 함.

기본적인 원칙은 복제 기능은 생성자와 팩터리를 이용하는 것이 최고.
단 배열은 clone 메서드 방식이 가장 깔끔한 예외.
