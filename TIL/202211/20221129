# 조회Bean이 2개이상인 경우

1. “@Autowired” 필드에 붙여준다
2. “@Qualifier 를 붙여주고 등록한 이름을 적어준다.
    1. 단점: 주입받을때 모든곳에 Bean을 붙여줘야함.
3. “@Primary"사용 - 편해서 가장 많이사용
    1. 이유: 간단하니까
    2. 자주사용하는 방식: 메인DB, 보조DB가 있을 때 메인DB에 붙여서 사용
