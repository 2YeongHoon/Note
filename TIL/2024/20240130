- 스프링에서 트랜잭션이 퍼블릭에서만 가능한 이유?
    
    트랜잭션 어노테이션을 클래스 전역으로 달아주게되면 모든 메서드에 적용되어 비용이 낭비된다.
    따라서 public에만 적용이 된다. 
    참고로 private에도 달리긴하지만 컴파일 오류는 발생하지 않음.
    
- 스프링 초기화 시에 트랜잭션이 안먹히는이유
    
    초기화 시점이 빈 주입 시점보다, aop가 늦음.
    

`@EventListener를 활용하면` 트랜잭션 가능
